class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // Observations - 
        // Alice can either pick
        //      - all the even indices OR
        //      - all the odd indices
        // And because the sumations is odd means sum(even) != sum(odd)
        // Hence alice always have the chance to pick the maximum
        // Therefore Alice always wins
        
        return true;
    }
};
